import { Inter } from "next/font/google";
import { Lusitana } from "next/font/google";
import { Chango, Satisfy } from "next/font/google";
import { Lobster } from "next/font/google";
export const inter = Inter({ subsets: ["latin"] });
export const lusitana = Lusitana({
  subsets: ["latin"],
  weight: ["400", "700"],
});
export const chango = Chango({ subsets: ["latin"], weight: ["400"] });
export const satisfy = Satisfy({ subsets: ["latin"], weight: ["400"] });

export const lobster = Lobster({ subsets: ["latin"], weight: ["400"] });
